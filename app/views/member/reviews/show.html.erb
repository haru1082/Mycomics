<div class="container">
  <div class="row justify-content-md-center mt-5">
    <div class="col-5 text-center border-bottom">
      <h4 class="px-4">投稿確認</h4>
    </div>
  </div>
  
  <% if @review.user == current_user %>
    <%= link_to "削除", review_path(@review), method: :delete %>
  <% end %>